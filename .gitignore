# ── OVMS runtime binaries (downloaded by update.ps1) ──────────────────────────
*.dll
*.exe
python/
thirdparty-licenses/

# ── Runtime-generated ─────────────────────────────────────────────────────────
cache/
*.log

# ── Captured HTTP request bodies (may contain user prompts) ───────────────────
captures/
vscode_*_request_body.json

# ── Local machine config (auto-generated by serve.ps1 on first run) ───────────
serve-config.json

# ── Backups created by update.ps1 ─────────────────────────────────────────────
backup_*/

# ── Claude Code session data ──────────────────────────────────────────────────
.claude/
